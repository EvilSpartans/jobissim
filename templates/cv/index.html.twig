{% extends 'base.html.twig' %}

{% block title %}CV{% endblock %}

{% block body %}
<section class="pt-4 pb-100">
    <div class="container-fluid">
        <div class="row">
            {{ include('cv/cv.html.twig') }}
            <div class="col-xl-12">
                <nav class="navigation pagination">
                    <div class="nav-links">
                        {{ include('cv/pagination.html.twig') }}
                    </div>
                </nav>
            </div>
        </div>
    </div>
</section>
{% endblock %}